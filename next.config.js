// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: '/gh-pages-test',
  experimental: {
    appDir: true,
  },
};

module.exports = nextConfig;
