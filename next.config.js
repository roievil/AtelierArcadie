// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: '/AtelierArcadie',
  experimental: {
    appDir: true,
  },
};

module.exports = nextConfig;
